# Minimal required environment variables for Auth0 integration

AUTH0_DOMAIN=your-auth0-domain
AUTH0_API_AUDIENCE=your-api-audience
# Paste JWKS JSON string from Auth0 dashboard (see docs)
AUTH0_JWKS='{"keys": [ ... ]}'

# Database settings (example, adjust as needed)
POSTGRES_USER=expense_user
POSTGRES_PASSWORD=your_password
POSTGRES_DB=expense_db
DATABASE_URL=postgresql://expense_user:your_password@localhost:5432/expense_db

# FastAPI secret key (for session/cookie security, not used by Auth0)
API_SECRET_KEY=your_secret_key
